.venv/
__pycache__/
*.pyc
.DS_Store
.env

# Large intermediate rasters — the committed fallbacks are the small derived files.
data/_cache/

# osmnx HTTP cache (Overpass response tiles) + the cached walk graph. Written by
# 04b_routed_access.py; re-downloadable. The committed result is the routed
# access_min/access_km already baked into the geojson/CSV.
cache/
pipeline/cache/

# Derived building mask for Bakersfield (32 MB). Regenerate with
#   python pipeline/02b_buildings.py --refresh
data/buildings_bakersfield.geojson
